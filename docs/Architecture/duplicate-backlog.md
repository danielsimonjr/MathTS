# Duplicate-symbol consolidation backlog

Honest inventory of the **genuine remaining runtime `TRUE_DUPLICATE`** work after the
2026-07 dedup campaign's compat-parity / bitwise / fft slice (`TRUE_DUPLICATE` 135 → 116).

- **Source of truth:** `docs/Architecture/duplicate-symbols.json` (regenerated by `npm run docs:deps`).
- **Accepted backlog snapshot:** `docs/Architecture/duplicate-baseline.json` (the `check:duplicates` gate
  fails on anything NEW beyond it).
- **Intentional-independence allowlist:** `tools/create-dependency-graph/duplicate-allowlist.json`.

This doc is the human-readable companion: it explains _why_ each remaining cluster is still flagged and
what the disposition is (consolidate / allowlist-as-dispatch-variant / **HUMAN-DECISION**), so the next
slice doesn't re-litigate the classification.

> Tag mechanics (from the finder): an entry flips from `TRUE_DUPLICATE` to `ALLOWLISTED` only when **all**
> its non-alias, non-`mathTyped`-dispatch definers are allowlisted. A `mathTyped('X', {...})` definer is
> auto-tagged `DISPATCH_VARIANT`, but that alone does not clear an entry unless **≥2** of the remaining
> definers are dispatch variants — so a `functions` typed wrapper delegating to a single `matrix` primitive
> (one dispatch + one plain body) still reads as `TRUE_DUPLICATE` until the `matrix` primitive is
> allowlisted or the wrapper is reclassified.

---

## 1. Matrix-domain routing (`functions` + `matrix`) — **RESOLVED 2026-07-18 (except `qr`)**

**Slice done (2026-07-18, matrix-domain routing slice, `TRUE_DUPLICATE` 104 → 89):** 15 of the 16
matrix-domain names were classified (by reading BOTH bodies) and resolved. Only `qr` remains — surfaced
as a keep-vs-route design call for Daniel (see below).

- **(a) Already dispatch-variants — ALLOWLISTED the matrix primitive.** `pinv`, `singularValues`,
  `normFro`, `lowRankApprox`, `matrixExpm`, `matrixLogm`, `matrixSqrtm`, and `cond`: the
  `functions/src/typed/matrix-ops.ts` `mathTyped(...)` wrappers were verified (call chain read) to route
  to the matrix-package primitives (`matrixCond`/`matrixPinv`/`matrixSingularValues`/… in
  `matrix/src/operations/{svd,pinv,expm,logm,sqrtm}.ts`). Matrix owns the single canonical body → no
  drift → allowlisted as `dispatch-variant`.
- **(b) Genuine independent body, routed to the matrix primitive.** `cholesky`: the
  `functions/src/typed/matrix-ops.ts` `number[][]` body now delegates to the matrix DenseMatrix
  primitive (native-accel), keeping only a symmetry pre-check + DenseMatrix→`number[][]` conversion.
  Pre-routing NumPy audit (`np.linalg.cholesky`, SPD corpus) confirmed BOTH bodies were already correct
  to full double precision — **no bug found**. Guarded by
  `functions/tests/gap-matrix-domain-dedup-parity.test.ts`.
- **(c/intra-package) `cond` second definer removed.** The shadowed power-iteration `cond` in
  `functions/src/typed/numeric.ts` was **dead** (unreachable via the public barrel — the `export *`
  collision + explicit override made matrix-ops' `cond` win; no internal caller) and was deleted at
  root. Public `cond` is unchanged (SVD-based matrix primitive).
- **Independent-by-design — ALLOWLISTED with parity guard.** `trace`, `diag`, `dotMultiply`, `row`,
  `column`, `subset`: `functions/` carries the mathjs-derived factory body (mathjs `Matrix`/`number[][]`
  contract) while `matrix/src/typed-operations.ts` dispatches the same op over `DenseMatrix` (different
  call contracts — e.g. `subset` takes a mathjs `index()` object vs matrix scalar `(row,col)`). The
  matrix typed-op definer is auto-tagged `DISPATCH_VARIANT`; the functions factory bodies (+ the
  `DenseMatrix.trace()` primitive in `matrix/src/types/dense/reduction.ts`) are allowlisted as
  intentional reimplementations, **pinned together** by
  `functions/tests/gap-matrix-domain-dedup-parity.test.ts` per [[feedback-allowlist-needs-parity-guard]].
- **`convolve`, `ifft`** were NOT part of this slice — they are fft-domain (Bucket D §5), not matrix-domain.

### `qr` — HUMAN-DECISION (surfaced 2026-07-18)

`qr` has two genuinely independent bodies with **different contracts**: `functions/src/algebra/decomposition/qr.ts`
(`createQr`, ~425 lines — mathjs factory over the mathjs `Matrix`/`SparseMatrix` bridge, **with a
WASM fast path** `qr_wasm`) vs `matrix/src/operations/qr.ts` (DenseMatrix primitive). Routing the factory
`qr` to the matrix layer (as `cholesky` was) would drop the mathjs Matrix contract AND the WASM path —
a real keep-vs-route ADR call, not a mechanical dedup. **Left on the backlog; surfaced for Daniel.**

### Original inventory (historical)

Names: `qr`, `cholesky`, `cond`, `pinv`, `trace`, `diag`, `row`, `column`, `subset`, `dotMultiply`,
`singularValues`, `normFro`, `matrixExpm`, `matrixLogm`, `matrixSqrtm`, `lowRankApprox`, `convolve`, `ifft`.

**Verified 2026-07 (read the bodies — not all of these are the same kind of duplicate):**

- **Already dispatch-variants** (a thin `functions` `mathTyped('X', {...})` wrapper that delegates to the
  canonical `matrix` primitive it imports): `normFro`, `pinv`, `lowRankApprox`, `singularValues`,
  `matrixExpm` (and the matrix-ops `cond`) in `functions/src/typed/matrix-ops.ts` import
  `matrixNormFro`/`matrixLowRankApprox`/`matrixSingularValues`/`matrixExpmPrimitive`/… and just call them;
  the `matrix/typed-operations.ts` group (`trace`/`diag`/`row`/`column`/`subset`/`dotMultiply`) is likewise
  the typed surface over `matrix`'s own operations. These are **not** copy-paste bodies — they are the
  intended native-accel dispatch pattern ([[project-two-decomposition-layers-prefer-matrix]], which routed
  `lup`/`qr`/`schur`/`slu` to the matrix layer on 2026-07-02). **Disposition:** a future slice should
  **allowlist the `matrix` primitive copy** with a `dispatch-variant: functions typed wrapper delegates to
the matrix primitive` reason (once each is spot-verified to actually delegate, per RFL R4). Deliberately
  **NOT** done in this slice — the compat/bitwise/fft slice explicitly scoped the matrix-domain set out to
  keep the parity-guard discipline focused.
- **Genuine independent bodies** (real consolidation candidates, not just re-tagging):
  - `cholesky` — `functions/src/typed/matrix-ops.ts` carries its **own** `number[][]` Cholesky (`export
function cholesky`), distinct from `matrix/src/operations/cholesky.ts`'s `DenseMatrix`-based one (the
    two `CholeskyResult` shapes are already allowlisted as different representations). Route the `functions`
    surface to the matrix primitive (accel pattern) rather than maintaining two factorizations.
  - `cond` — **three** definers: `functions/src/typed/matrix-ops.ts` (`mathTyped`, delegates to
    `matrix` svd) **and** `functions/src/typed/numeric.ts` (`export function cond(A: number[][]): f64`, an
    independent body) **and** `matrix/src/operations/svd.ts`. The two `functions`-internal `cond`s are a
    real intra-package duplication to reconcile first.
  - `convolve`, `ifft` — `functions` signal path vs `matrix/src/backends/wasm/fft-wasm.ts`; part of the
    Bucket-D domain-FFT case-by-case (see §5 note below), needs the canonical-vs-dead-WASM-bridge decision.

**Disposition:** consolidate the genuine independent bodies (`cholesky`, the `functions`-internal `cond`)
onto the canonical primitive; allowlist the confirmed delegating wrappers as `dispatch-variant` in a
follow-up slice. No behavior change expected — verify against the matrix-layer oracle-pinned tests.

## 2. Transcendental temperature-split (`core` + `functions`)

Names: `sinh`, `cosh`, `tanh`, `asinh`, `acosh`, `atanh`, `cbrt`, `log2`, `log10`, `log1p`, `expm1`, `sign`.

The established Bucket-C pattern: `core/src/number.ts` (and `core/src/arithmetic/scalar.ts`) own the
**hot `number` primitive** (kept inline for V8 inlining), and `functions/src/typed/{arithmetic,trigonometry}.ts`
register a `mathTyped` dispatch that handles the **rich types** (`BigNumber`/`Complex`/`Fraction`) and should
delegate the `number` case to the core primitive. This is the same temperature-split already applied to
`round`/`pow`/`equal` (Bucket C slice 1, which fixed 3 real BigNumber bugs). **Disposition:** delegate the
rich-type `number` case to core (perf-free — the method-call cost on a boxed type dwarfs a cross-module
call), edge-case-corpus + manifest-test guarded, per the ledger's Bucket-C policy. Backlog, not this slice.

## 3. WasmLoader security cluster (`functions` + `matrix`) — **HUMAN-DECISION (ADR)**

Names: `WasmLoader`, `wasmLoader`, `loadWasmManifest`, `sha384OfBuffer`, `verifyWasmIntegrity`,
`resolvePackagedWasm`, `defaultWasmLocation`.

Each package resolves **its own** `mathts-as.wasm` path and enforces the **SHA-384 manifest verification**
security invariant (CLAUDE.md "Security Invariants" #1) before compile/instantiate. `matrix` cannot import
from `functions` (that would invert the dependency edge and create a cycle). The adversarial reviewers
recommended a **shared `core` loader with injected path resolution** (both packages reach `core` without a
cycle), but this touches a **security-critical** verification path — it is an **ADR-level call** (a wrong
refactor here silently weakens integrity checking, exactly the class of change CLAUDE.md says must not
regress). **Leave as `TRUE_DUPLICATE`; surfaced for a human ADR decision.** Do not fold into a routine
dedup slice.

## 4. workerpool capability caps / fork-integration #27 — **HUMAN-DECISION**

Names: `canUseWasm`, `canUseSharedMemory` (`packages/workerpool/src/index.ts` vs
`…/workerpool-browser-shim.ts`), `initializePool`, `terminatePool` (`functions/src/typed/arithmetic.ts` vs
`packages/workerpool/src/index.ts`).

Both adversarial reviewers recommended merging the workerpool capability duplication now (~½ day), but this
is entangled with **#27 (fork first-party integration)**, which Daniel **PAUSED**
([[project-fork-first-party-integration]]). The `index.ts`-vs-`browser-shim.ts` pair is an intentional
environment split (node vs browser build target). **Disposition: HUMAN-DECISION — do not touch until #27 is
un-paused.**

## 5. Other residual clusters (smaller, mixed)

- **`bitXor`** stays `TRUE_DUPLICATE` even after the bitwise allowlist: its third definer
  `functions/src/utils/bignumber/bitwise.ts` (the BigNumber bit op) is a separate `functions`-internal
  concern from the `functions/typed` ↔ `parallel` dispatch pair. Reconcile the BigNumber bitwise layer
  separately.
- **`fft`/`ifft`/`rfft` (Bucket D domain-FFT):** `functions/src/signal/fft.ts` (Complex-object) vs
  `matrix/src/backends/wasm/fft-wasm.ts` (split Float64Array) vs `assembly/src/ops/fft.ts` (AS mirror,
  already allowlisted). Case-by-case: pick the canonical, redirect, retire the unreachable matrix WASM-FFT
  bridge if dead. Distinct from the `fftshift`/`ifftshift` roll helpers consolidated this slice.
- **compat factory homonyms** (`bignumber`/`fraction`/`matrix`/`sparse`/`complex`/`identity`/`zeros`/`ones`/
  `size`/`transpose`) and **core+functions scalar factories** (`addScalar`/`subtractScalar`/
  `multiplyScalar`/`divideScalar`/`number`/`isNumeric`/`isInteger`/…): a mix of compat mathjs-factory
  surfaces and the core↔functions factory-scope split — folded into the Bucket-C layer-collapse ADR, not
  quick wins.

---

_Last updated: 2026-07-18 (matrix-domain routing slice; `TRUE_DUPLICATE` 104 → 89; only `qr` of the
matrix-domain set remains, surfaced for a human keep-vs-route decision)._
