# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
out/
*.tsbuildinfo

# Python bytecode (inventory/scan tooling under tools/)
__pycache__/
*.pyc

# WASM build artifacts
assembly/build/
*.wasm
!packages/*/src/**/*.wasm

# Test & coverage
coverage/
.nyc_output/

# IDE & editors
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# OS files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment
.env
.env.local
.env.*.local
*.local

# Cache
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
*.tscache

# Temporary files
tmp/
temp/
*.tmp
*.temp

# Package manager locks (keep one)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Workbook outputs (optional - can be tracked)
*.mtsw.outputs.yaml
.mtsw-cache/

# Benchmark results (generated, machine-specific)
benchmark-results/
tools/benchmark/parallel/results.json

# Documentation build
docs/_site/
docs/.docusaurus/

# Release artifacts
*.tgz
*.tar.gz

# Claude Code
.claude/
.mcp.json

# Turbo
.turbo/

# Note: packages/typed-function/ and packages/workerpool/ are now included in this repo
.mcp.txt
CUsersdanieAppDataLocalTempclaudecoverage.txt
.npmrc
tools/benchmark/gpu/bench-3way.bundle.js

# Merge-conflict debris. Two of these (648 lines) reached main via #237, and #238
# was closed partly for committing a third. Nothing ignored them, so nothing stopped
# them.
*.orig
*.rej

# Orphaned declaration source maps. 513 of these were committed under package src/
# dirs (none had a matching .d.ts). .gitignore only covered dist/, so anything a
# build left in src/ was committable. NOT *.d.ts - those are real ambient decls.
*.d.ts.map
*.js.map
