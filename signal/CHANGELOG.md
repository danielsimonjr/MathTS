# @danielsimonjr/mathts-signal

## 0.1.0

### Minor Changes

- Initial release. Exposes the MathTS signal-processing functions as a focused package that re-exports the 34 `signal` typed-function operations from `@danielsimonjr/mathts-functions` (pinned `0.2.4`). Not a copy.
