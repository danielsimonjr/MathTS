# @danielsimonjr/mathts-arithmetic

## 0.1.0

### Minor Changes

- Initial release. Exposes the MathTS arithmetic functions as a focused package that re-exports the 46 `arithmetic` typed-function operations from `@danielsimonjr/mathts-functions` (pinned `0.2.4`). Not a copy.
