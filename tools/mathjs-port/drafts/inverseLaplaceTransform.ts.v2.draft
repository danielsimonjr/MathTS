// PORT BLOCKED: The HARD CONSTRAINTS require (1) internal helpers operate on Node objects not strings, (2) NO numerical-pattern-matching shortcuts, and (3) mirror mathjs's algorithm exactly — but mathjs's actual implementation (as provided in the source) uses numerical evaluation (_evalAt, _matchPattern, _matchQuadratic, _matchPower, _matchExponential, _matchScaledExponential) and string-level operations (_tableLookup calls node.toString()) throughout its "node-level" helpers. The algorithm IS numerical pattern matching against evaluated values, not AST walking. The constraints are mutually contradictory: faithfully mirroring mathjs's exact algorithm requires numerical pattern matching, which the hard constraints forbid. A port that satisfies all hard constraints simultaneously is impossible.